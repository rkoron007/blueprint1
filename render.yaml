services:
  - type: web
    name: disk-mount-test-app
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
    # Uncomment and configure disk after deploying and checking pwd:
    # disks:
    #   - name: test-disk
    #     mountPath: /opt/render/project/src/data
    #     sizeGB: 1
